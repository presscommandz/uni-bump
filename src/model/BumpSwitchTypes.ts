enum BumpSwitchType {
    major = "--major",
    minor = "--minor",
    patch = "--patch",
    build = "--build",
    newVersion = "--new-version",
    commitMessage = "--message"
}

export default BumpSwitchType
