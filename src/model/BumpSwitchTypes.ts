enum BumpSwitchType {
    major = "--major [number]",
    minor = "--minor [number]",
    patch = "--patch [number]",
    build = "--build [number]",
    newVersion = "--new-version <version>"
}

export default BumpSwitchType
